# Cursor SDK
# Optional server-side fallback for hosted demos (local keys take precedence via headers)
CURSOR_API_KEY=
CURSOR_MODEL=composer-2.5

# GitHub — optional server-side fallback for preview and export
GITHUB_TOKEN=
TEMPLATE_REPO_URL=https://github.com/AppleLamps/cursor-sdk-web-template
TEMPLATE_REPO_REF=main
